{% extends 'base.html.twig' %}
{% block title %}NyAhy connexion{% endblock %}


{% block body %}


{# {% if error %}
    <div class="alert alert-danger">
        {{ error.messageKey|trans(error.messageData, 'security') }}
    </div>
{% endif %} #}
<body id="body_login_user">
  <div class="container">
    <div class="row">
      <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
        <div class="card border-0 shadow rounded-3 my-5">
          <div class="card-body p-4 p-sm-5">
            <div class="div_loginRecruteur">
                <h5 class="card-title text-center mb-5 fw-light fs-2">Connexion</h5>
                {% for flashMessage in app.flashes('error') %}
                  <div class="alert alert-danger">{{ flashMessage }}</div>
                {% endfor %}
            </div>
             <form action="{{path('app_connexion_user')}}" method="post">
                <div class="form-group m-2">
                    <input placeholder="e-mail" required name="_email" type="text" class="form-control">
                </div>
                <div class="form-group m-2">
                    <input placeholder="mot de passe" required name="_password" type="password" class="form-control">
                </div>
                <div class="div_loginUser">
                  <button class="btn_login btn btn-primary" type="submit">connecter</button>
                </div>
                <hr>
                <div>
                  <a href="{{path('app_inscription_user')}}"><p> Je n'ai pas de compte</p></a>
                </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>

{# 
                <hr class="my-4">
              <div class="d-grid mb-2">
                <button class="btn btn-google btn-login text-uppercase fw-bold" type="submit">
                  <i class="fab fa-google me-2"></i> Sign in with Google
                </button>
              </div>
              <div class="d-grid">
                <button class="btn btn-facebook btn-login text-uppercase fw-bold" type="submit">
                  <i class="fab fa-facebook-f me-2"></i> Sign in with Facebook
                </button>
              </div> #}
{% endblock %}

